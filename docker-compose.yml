version: '3.8'

services:
  hnf-crm-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hnf_crm_app
    ports:
      - "8080:80"
    restart: always
    environment:
      - NODE_ENV=production
