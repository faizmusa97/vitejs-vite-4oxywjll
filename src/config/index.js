// ===================================================
// HNF CRM Configuration Module
// ===================================================

export const config = {
  apiBaseUrl: import.meta.env?.VITE_API_BASE_URL || 'api',
};

export default config;
